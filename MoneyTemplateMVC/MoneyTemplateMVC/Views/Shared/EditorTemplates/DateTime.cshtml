
@model DateTime?
@{
    var format = ViewData["Format"]?.ToString() ?? "yyyy-mm-dd";

    var value = Model?.ToString(format) ?? "";

}
@Html.TextBox("", value, new
{
    @class = "form-control",
    data_datetimepicker = "y",
    data_value = format,
})